{
  //connectionString, storing sensetive information is the source code is not safe, should saved out of here, stored as an enviroment variable using SecretManager, config in project-vage.csproj and program files
  // "ConnectionString": {
  //   "Default": "server=localhost; database=vega; Integrated Security=SSPI"
  // },

  "PhotoSettings": {
    "MaxBytes": 10485760,
    "AcceptedFileTypes":  [".jpg", ".jpeg", ".png"]
  },

  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Default": "Warning"
      }
    }
  }
}
